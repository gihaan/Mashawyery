<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gihan.mashawyery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="com.google.android.providers.gaf.permission.READ_GSERVICES"/>



    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">



        <activity
            android:name=".ui.First">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

          <!-- optional (needed if default theme has no action bar) -->

        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.Regestration" />
        <activity android:name=".ui.Login" />
        <activity android:name=".ui.PaymentActivity" />
        <activity android:name=".ui.TripsActivity" />
        <activity android:name=".ui.SettingActivity" />
        <activity android:name=".ui.HelpActivity" />
        <activity android:name=".ui.CallUsActivity" />
        <activity android:name=".ui.PersonalActivity" />
        <activity android:name=".ui.CashActivity" />
        <activity android:name=".ui.CreditActivity" />
        <activity android:name=".ui.DetailTripActivity" />
        <activity android:name=".ui.VerificationPhoneNumber" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.MainMap"
            android:label="@string/title_activity_main_map"></activity>
    </application>

</manifest>